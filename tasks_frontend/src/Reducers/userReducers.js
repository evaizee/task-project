const initialState = {
    name: "flare",
    id: 1,
    email: "ro@ro.co",
}

export default function(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
}